{% extends 'base.html.twig' %}

{% block title %}Calendrier{% endblock %}

{% block body %}
    {% include '_menu.html.twig' %}
    <h1 class="text-center">Calendrier</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Intitulé :</th>
                <td>{{ calendrier.title }}</td>
            </tr>
            <tr>
                <th>Commence le :</th>
                <td>{{ calendrier.beginAt ? calendrier.beginAt|date('d-m-Y H:i') : '' }}</td>
            </tr>
            <tr>
                <th>Finit le :</th>
                <td>{{ calendrier.endAt ? calendrier.endAt|date('d-m-Y H:i') : '' }}</td>
            </tr>

        </tbody>
    </table>

    <a href="{{ path('calendrier_index') }}"><button class="btn btn-success">Retour</button></a>

    <a href="{{ path('calendrier_edit', {'id': calendrier.id}) }}">Modifier</a>

    {{ include('calendrier/_delete_form.html.twig') }}
{% endblock %}
